# Pokedex-Replica
[Study Project]

Classic red Pokédex in plain HTML, CSS and JavaScript. Pulls Pokémon from [PokéAPI](https://pokeapi.co/), 20 per page, and shows sprite, types, height and weight for the selected one.

Open `index.html` in a browser, no build step.
